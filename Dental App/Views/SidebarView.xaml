<UserControl x:Class="Dental_App.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#101926">
    <StackPanel Margin="10">
        <TextBlock Text="DentaCare Pro" Foreground="#2ECC71" FontSize="20" FontWeight="Bold" Margin="0,0,0,30"/>

        <Button Content="Dashboard" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Foreground="White" HorizontalAlignment="Left" Margin="0,5"/>
        <Button Content="Patients" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Foreground="White" HorizontalAlignment="Left" Margin="0,5"/>
        <Button Content="Odontogramme" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Foreground="White" HorizontalAlignment="Left" Margin="0,5"/>
    </StackPanel>
</UserControl>