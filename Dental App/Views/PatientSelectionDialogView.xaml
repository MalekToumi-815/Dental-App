<UserControl x:Class="Dental_App.Views.PatientSelectionDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource PrimaryBackground}"
             Width="500" Height="400">

    <UserControl.Resources>
        <Style x:Key="SearchBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <!-- Improved ListBoxItem Style -->
        <Style x:Key="PatientListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4"
                                Margin="0,1,0,1">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SecondaryBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Improved Button Styles -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackground}" Padding="20,14" BorderBrush="{DynamicResource GlobalElementBorderBrush}" BorderThickness="2">
            <StackPanel>
                <TextBlock Text="Selectionner un Patient" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource TextPrimary}" 
                           Margin="0,0,0,4"/>
                <TextBlock Text="Recherchez le patient" 
                           FontSize="12" 
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>
        </Border>

        <!-- Search Box Section -->
        <StackPanel Grid.Row="1" Margin="20,12,20,12">
            <Border Style="{StaticResource SearchBoxStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Path Data="M9.5,2A7.5,7.5 0 0,1 17,9.5A7.5,7.5 0 0,1 9.5,17A7.5,7.5 0 0,1 2,9.5A7.5,7.5 0 0,1 9.5,2M9.5,4A5.5,5.5 0 0,0 4,9.5A5.5,5.5 0 0,0 9.5,15A5.5,5.5 0 0,0 15,9.5A5.5,5.5 0 0,0 9.5,4M14.5,13L21,19.5L19.5,21L13,14.5Z"
                          Fill="{DynamicResource TextSecondary}" Stretch="Uniform" Width="18" Margin="0,0,10,0"/>
                    <TextBox Grid.Column="1" 
                             Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent" 
                             BorderThickness="0" 
                             VerticalAlignment="Center"
                             Foreground="{DynamicResource TextPrimary}"
                             Padding="8,0"/>
                </Grid>
            </Border>
            <TextBlock Text="{Binding PatientCount, StringFormat='Resultats: {0}'}" 
                       Foreground="{DynamicResource TextSecondary}" 
                       FontSize="11" 
                       Margin="0,6,0,0"/>
        </StackPanel>

        <!-- Patient List Container -->
        <Border Grid.Row="2" Background="{DynamicResource CardBackground}" Margin="20,0,20,12" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource GlobalElementBorderBrush}">
            <Grid>
                <!-- List with patients -->
                <ListBox x:Name="PatientListBox"
                         ItemsSource="{Binding FilteredPatients}"
                         SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                         ItemContainerStyle="{StaticResource PatientListItemStyle}"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="8"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Patient Info -->
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FullName}" 
                                               FontSize="13" 
                                               FontWeight="SemiBold" 
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"/>
                                    <TextBlock FontSize="11" 
                                               Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                               Opacity="0.7"
                                               Margin="0,3,0,0">
                                        <Run Text="Tel: "/>
                                        <Run Text="{Binding Telephone, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- Patient ID Badge -->
                                <Border Grid.Column="1" 
                                        Background="{DynamicResource BorderColor}" 
                                        CornerRadius="4"
                                        Padding="8,4"
                                        VerticalAlignment="Center"
                                        Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}" 
                                               FontSize="10" 
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimary}"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- No Results Message -->
                <Grid Visibility="{Binding IsNoResultsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="Aucun patient trouve" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextSecondary}" 
                                   TextAlignment="Center" 
                                   Margin="0,0,0,6"/>
                        <TextBlock Text="Essayez un autre nom" 
                                   FontSize="11" 
                                   Foreground="{DynamicResource BorderColor}" 
                                   TextAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Loading State -->
                <TextBlock Visibility="{Binding IsLoadingPatients, Converter={StaticResource BooleanToVisibilityConverter}}"
                           Text="Chargement..." 
                           FontSize="12" 
                           Foreground="{DynamicResource TextSecondary}" 
                           TextAlignment="Center"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="3"  Padding="20,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding CancelCommand}"
                        Margin="0,0,8,0"/>
                <Button Content="Selectionner" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding SelectCommand}"
                        IsEnabled="{Binding IsPatientSelected}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
