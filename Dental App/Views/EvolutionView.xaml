<UserControl x:Class="Dental_App.Views.EvolutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource PrimaryBackground}">

    <UserControl.Resources>
        <!-- Reuse styles similar to CaisseView for consistent look -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlobalElementBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid Margin="12,0">
                                <TextBlock x:Name="Placeholder"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="{TemplateBinding FontSize}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TealBtn" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryBackground}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small label style consistent with other views -->
        <Style x:Key="SmallLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Small style to remove list selection visuals when used -->
        <Style x:Key="ChartListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>

        <!-- Ensure ItemsControl content presenters align their content to the bottom so all bars share same baseline -->
        <Style x:Key="ChartItemContainerStyle" TargetType="ContentPresenter">
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Button style used to make template clickable without chrome; keep content aligned to bottom -->
        <Style x:Key="TransparentButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Bottom"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Inputs: keep only Year input -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Annee"  FontWeight="Bold" Style="{StaticResource SmallLabel}" Margin="0,0,0,12" />
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="120" Style="{StaticResource InputStyle}" Tag="Annee" Text="{Binding Year, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Mettre a jour" Command="{Binding UpdateCommand}" Style="{StaticResource TealBtn}" Margin="12,0,0,0" Padding="12,6" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Monthly histogram: grouped bars (revenue vs depense) -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource GlobalElementBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12"  MinHeight="360">
                <!-- Use Grid so chart area gets constrained height (StackPanel gave infinite height) -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="Courbe mensuelle (12 mois)" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,20,0"/>
                        <TextBlock Text="Evolution mensuelle" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" FontSize="16"/>
                        <TextBlock Text="Montants par mois (R: Revenu, D: Depense, Net)" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    </StackPanel>

                    <!-- Legend -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,12">
                        <Border Background="{DynamicResource AccentColor}" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Revenu" Foreground="{DynamicResource TextSecondary}" Margin="0,0,12,0"/>
                        <Border Background="#FF6B6B" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Depense" Foreground="{DynamicResource TextSecondary}" Margin="0,0,12,0"/>
                        <Border Background="#FFA500" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Net" Foreground="{DynamicResource TextSecondary}"/>
                    </StackPanel>

                    <!-- Chart area constrained by Grid.Row=2 -->
                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" CanContentScroll="False">
                        <ItemsControl ItemsSource="{Binding MonthlyData}" ItemContainerStyle="{StaticResource ChartItemContainerStyle}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Template similar to Caisse: bars on top, values below, month name at bottom -->
                                    <StackPanel Margin="12,0" Width="120" VerticalAlignment="Bottom">
                                        <!-- Bars: now include Net bar as third bar -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,6">
                                            <Border Width="30" Height="{Binding RevenueHeight}" Background="{DynamicResource AccentColor}" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Revenue}"/>
                                            <Border Width="8" Background="Transparent"/>
                                            <Border Width="30" Height="{Binding ExpenseHeight}" Background="#FF6B6B" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Expense}"/>
                                            <Border Width="8" Background="Transparent"/>
                                            <Border Width="22" Height="{Binding NetHeight}" Background="#FFA500" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Net}"/>
                                        </StackPanel>

                                        <!-- Values under bars -->
                                        <TextBlock Text="{Binding Revenue, StringFormat='R: {0:N0}'}" Foreground="{DynamicResource AccentColor}" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Expense, StringFormat='D: {0:N0}'}" Foreground="#FF6B6B" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Net, StringFormat='Net: {0:N0}'}" Foreground="#FFA500" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>

                                        <!-- Month label -->
                                        <TextBlock Text="{Binding Label}" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Yearly histogram: grouped bars per year -->
            <Border Grid.Column="1" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource GlobalElementBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12"  MinHeight="360" Margin="12,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="Courbe annuelle (4 ans)" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Evolution annuelle" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" FontSize="16"/>
                        <TextBlock Text="Totaux annuels (R: Revenu, D: Depense, Net)" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,12">
                        <Border Background="{DynamicResource AccentColor}" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Revenu" Foreground="{DynamicResource TextSecondary}" Margin="0,0,12,0"/>
                        <Border Background="#FF6B6B" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Depense" Foreground="{DynamicResource TextSecondary}" Margin="0,0,12,0"/>
                        <Border Background="#FFA500" Width="12" Height="12" CornerRadius="3" Margin="0,0,6,0"/>
                        <TextBlock Text="Net" Foreground="{DynamicResource TextSecondary}"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" CanContentScroll="False">
                        <ItemsControl ItemsSource="{Binding YearlyData}" ItemContainerStyle="{StaticResource ChartItemContainerStyle}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Wrap in a transparent Button so clicking a year updates charts -->
                                    <Button Style="{StaticResource TransparentButton}" Command="{Binding DataContext.SelectYearCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Year}">
                                        <StackPanel Margin="14,0" Width="180" VerticalAlignment="Bottom">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,6">
                                                <Border Width="40" Height="{Binding RevenueHeight}" Background="{DynamicResource AccentColor}" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Revenue}"/>
                                                <Border Width="10" Background="Transparent"/>
                                                <Border Width="40" Height="{Binding ExpenseHeight}" Background="#FF6B6B" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Expense}"/>
                                                <Border Width="10" Background="Transparent"/>
                                                <Border Width="28" Height="{Binding NetHeight}" Background="#FFA500" CornerRadius="6" VerticalAlignment="Bottom" ToolTip="{Binding Net}"/>
                                            </StackPanel>

                                            <TextBlock Text="{Binding Revenue, StringFormat='R: {0:N0}'}" Foreground="{DynamicResource AccentColor}" FontSize="13" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding Expense, StringFormat='D: {0:N0}'}" Foreground="#FF6B6B" FontSize="13" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding Net, StringFormat='Net: {0:N0}'}" Foreground="#FFA500" FontSize="13" HorizontalAlignment="Center" Margin="0,4,0,0"/>

                                            <TextBlock Text="{Binding Year}" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
