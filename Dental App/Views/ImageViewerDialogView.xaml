<UserControl x:Class="Dental_App.Views.ImageViewerDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">

    <UserControl.Resources>
        <!-- SVG Icon Button Style -->
        <Style x:Key="DialogIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="MinWidth" Value="44"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2a2a2a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Dialog View -->
        <Border Background="{DynamicResource CardBackground}"
                CornerRadius="16"
                BorderBrush="{DynamicResource GlobalElementBorderBrush}"
                BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Top Controls Bar -->
                <Grid Grid.Row="0" Margin="16,12,16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Close Button -->
                    <Button Grid.Column="1"
                            Style="{StaticResource DialogIconButtonStyle}"
                            Command="{Binding CloseImageViewerCommand}"
                            ToolTip="Close">
                        <Path Data="M19 6.41L17.59 5L12 10.59L6.41 5L5 6.41L10.59 12L5 17.59L6.41 19L12 13.41L17.59 19L19 17.59L13.41 12L19 6.41Z"
                              Fill="{DynamicResource TextPrimary}" 
                              Width="20" Height="20"/>
                    </Button>
                </Grid>

                <!-- Image -->
                <Border Grid.Row="1" Margin="16" CornerRadius="12" Background="{DynamicResource SecondaryBackground}" ClipToBounds="True">
                    <Image Source="{Binding SelectedImage.ImagePath}" 
                           Stretch="Uniform"/>
                </Border>

                <!-- Info Section -->
                <StackPanel Grid.Row="2" Margin="20,12,20,20">
                    <TextBlock Text="{Binding SelectedImage.ImageType}" 
                               Foreground="{DynamicResource TextPrimary}" 
                               FontSize="14" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <TextBlock Foreground="{DynamicResource TextSecondary}" 
                               FontSize="12">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Date: {0:yyyy-MM-dd}">
                                <Binding Path="SelectedImage.DateTaken"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>

</UserControl>
