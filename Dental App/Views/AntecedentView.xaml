<UserControl x:Class="Dental_App.Views.AntecedentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:Dental_App.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             Background="{DynamicResource PrimaryBackground}">

	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BoolToVis"/>
		<local:InverseBooleanConverter x:Key="InverseBoolToVis"/>

		<!-- Style TextBox -->
		<Style x:Key="InputStyle" TargetType="TextBox">
			<Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
			<Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
			<Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="Height" Value="44"/>
			<Setter Property="CaretBrush" Value="{DynamicResource AccentColor}"/>
			<Setter Property="Padding" Value="12,0"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="TextBox">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
							<Grid Margin="12,0">
								<TextBlock x:Name="Placeholder"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="{TemplateBinding FontSize}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
								<ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
							</Grid>
						</Border>
						<ControlTemplate.Triggers>
							<MultiTrigger>
								<MultiTrigger.Conditions>
									<Condition Property="Text" Value=""/>
									<Condition Property="IsKeyboardFocused" Value="False"/>
								</MultiTrigger.Conditions>
								<Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
							</MultiTrigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Bouton Teal -->
		<Style x:Key="TealBtn" TargetType="Button">
			<Setter Property="Background" Value="{DynamicResource AccentColor}"/>
			<Setter Property="Foreground" Value="{DynamicResource PrimaryBackground}"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="14"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Bd" Property="Opacity" Value="0.8"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Bouton Icon -->
		<Style x:Key="IconBtn" TargetType="Button">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Width" Value="32"/>
			<Setter Property="Height" Value="32"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="Bd" Property="Background" Value="{DynamicResource CardBackground}"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<Grid>
		<Grid Margin="32,28">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="24"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="16"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="16"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<!-- Header -->
			<Grid Grid.Row="0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<StackPanel VerticalAlignment="Center">
					<TextBlock Text="Antecedents" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
					<TextBlock Text="{Binding PatientDisplayName}" Foreground="{DynamicResource TextSecondary}" FontSize="14" Margin="0,4,0,0"/>
				</StackPanel>

				<Button Grid.Column="1" Style="{StaticResource TealBtn}" Padding="20,0" Height="42" Command="{Binding OpenModalCommand}">
					<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
						<TextBlock Text="+" FontSize="20" FontWeight="Bold" Margin="0,0,8,2" VerticalAlignment="Center"/>
						<TextBlock Text="Nouvel Antecedent" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
					</StackPanel>
				</Button>
			</Grid>

			<!-- Patient Search -->
			<Border Grid.Row="2" Background="{DynamicResource SecondaryBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<Grid Grid.Column="0">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<TextBox Grid.Row="0" Style="{StaticResource InputStyle}" Tag="Search patient by name..." Text="{Binding PatientSearchText, UpdateSourceTrigger=PropertyChanged}" Height="44" Margin="0,0,0,0"/>

						<Border Grid.Row="1" Background="{DynamicResource SecondaryBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,1,0,0" Visibility="{Binding HasPatients, Converter={StaticResource BoolToVis}}" MaxHeight="200">
							<ScrollViewer VerticalScrollBarVisibility="Auto">
								<ItemsControl ItemsSource="{Binding Patients}" HorizontalContentAlignment="Stretch">
									<ItemsControl.ItemTemplate>
										<DataTemplate>
											<Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="16,12" Background="Transparent">
												<Border.Style>
													<Style TargetType="Border">
														<Setter Property="Background" Value="Transparent"/>
														<Style.Triggers>
															<Trigger Property="IsMouseOver" Value="True">
																<Setter Property="Background" Value="{DynamicResource CardBackground}"/>
															</Trigger>
														</Style.Triggers>
													</Style>
												</Border.Style>
												<Grid>
													<Grid.InputBindings>
														<MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.SelectPatientCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
													</Grid.InputBindings>
													<StackPanel VerticalAlignment="Center">
														<TextBlock Text="{Binding Nom}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="14"/>
														<TextBlock Text="{Binding Prenom, StringFormat='Prenom: {0}'}" Foreground="{DynamicResource TextSecondary}" FontSize="12" Margin="0,4,0,0"/>
													</StackPanel>
												</Grid>
											</Border>
										</DataTemplate>
									</ItemsControl.ItemTemplate>
								</ItemsControl>
							</ScrollViewer>
						</Border>
					</Grid>

					<Button Grid.Column="1" Style="{StaticResource TealBtn}" Command="{Binding ClearPatientSearchCommand}" Width="44" Height="44" Margin="8,0,0,0" ToolTip="Clear">
						<TextBlock Text="X" Foreground="{DynamicResource PrimaryBackground}" FontSize="18" FontWeight="Bold"/>
					</Button>
				</Grid>
			</Border>

			<!-- Selected Patient Info -->
			<Border Grid.Row="4" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Padding="16,12" Visibility="{Binding HasSelectedPatient, Converter={StaticResource BoolToVis}}">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<StackPanel Grid.Column="0" VerticalAlignment="Center">
						<TextBlock Text="Selected patient:" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
						<TextBlock Text="{Binding SelectedPatientName}" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="Bold" Margin="0,4,0,0"/>
					</StackPanel>

					<TextBlock Grid.Column="1" Text="{Binding AntecedentsCount, StringFormat='Total: {0}'}" Foreground="{DynamicResource TextSecondary}" FontSize="12" VerticalAlignment="Center"/>
				</Grid>
			</Border>

			<!-- Antecedents Search -->
			<StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,0,0,8" Visibility="{Binding HasSelectedPatient, Converter={StaticResource BoolToVis}}">
				<TextBox Style="{StaticResource InputStyle}" Tag="Search antecedent..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250"/>

				<Button Style="{StaticResource TealBtn}" Command="{Binding ClearSearchCommand}" Width="44" Height="44" Margin="8,0,0,0" ToolTip="Clear">
					<TextBlock Text="X" Foreground="{DynamicResource PrimaryBackground}" FontSize="18" FontWeight="Bold"/>
				</Button>
			</StackPanel>

			<!-- Antecedents Table -->
			<Border Grid.Row="6" Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="12" ClipToBounds="True" Margin="0,50,0,0">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="*"/>
					</Grid.RowDefinitions>

					<!-- Header -->
					<Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="20,14" Background="{DynamicResource PrimaryBackground}">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="50"/>
								<ColumnDefinition Width="200"/>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="120"/>
							</Grid.ColumnDefinitions>
							<TextBlock Grid.Column="0" Text="#" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold"/>
							<TextBlock Grid.Column="1" Text="NOM" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold"/>
							<TextBlock Grid.Column="2" Text="DESCRIPTION" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold"/>
							<TextBlock Grid.Column="3" Text="ACTIONS" Foreground="{DynamicResource TextSecondary}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center"/>
						</Grid>
					</Border>

					<!-- Rows -->
					<ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
						<ItemsControl x:Name="AntecedentList" ItemsSource="{Binding Antecedents}" HorizontalContentAlignment="Stretch">
							<ItemsControl.ItemTemplate>
								<DataTemplate>
									<Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="20,16">
										<Border.Style>
											<Style TargetType="Border">
												<Setter Property="Background" Value="Transparent"/>
												<Style.Triggers>
													<Trigger Property="IsMouseOver" Value="True">
														<Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
													</Trigger>
												</Style.Triggers>
											</Style>
										</Border.Style>
										<Grid>
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="50"/>
												<ColumnDefinition Width="200"/>
												<ColumnDefinition Width="*"/>
												<ColumnDefinition Width="120"/>
											</Grid.ColumnDefinitions>

											<TextBlock Grid.Column="0" Text="{Binding Id}" Foreground="{DynamicResource TextSecondary}" FontSize="14" VerticalAlignment="Center"/>
											<TextBlock Grid.Column="1" Text="{Binding Nom}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
											<TextBlock Grid.Column="2" Text="{Binding Description}" Foreground="{DynamicResource TextSecondary}" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Center"/>

											<StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
												<Button Style="{StaticResource IconBtn}" Margin="0,0,6,0" ToolTip="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
													<Path Data="M3 17.25V21h3.75L17.81 9.94l-3.75-3.75zM20.71 7.04a1 1 0 000-1.41l-2.34-2.34a1 1 0 00-1.41 0l-1.83 1.83 3.75 3.75z" Fill="{DynamicResource AccentColor}" Width="15" Height="15" Stretch="Uniform"/>
												</Button>
												<Button Style="{StaticResource IconBtn}" ToolTip="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
													<Path Data="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z" Fill="#EF4444" Width="15" Height="15" Stretch="Uniform"/>
												</Button>
											</StackPanel>
										</Grid>
									</Border>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</ScrollViewer>

					<!-- Empty Message -->
					<TextBlock Grid.Row="1" Text="No antecedent registered" Foreground="{DynamicResource TextSecondary}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasAntecedents, Converter={StaticResource InverseBoolToVis}}"/>
				</Grid>
			</Border>
		</Grid>

		<!-- MODAL -->
		<Grid Visibility="{Binding IsModalOpen, Converter={StaticResource BoolToVis}}">
			<Rectangle Fill="#000000" Opacity="0.65"/>

			<Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="14" Width="500" Padding="30,26" HorizontalAlignment="Center" VerticalAlignment="Center">
				<StackPanel>
					<Grid Margin="0,0,0,22">
						<TextBlock Text="{Binding ModalTitle}" Foreground="{DynamicResource TextPrimary}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
						<Button HorizontalAlignment="Right" Style="{StaticResource IconBtn}" Command="{Binding CloseModalCommand}" ToolTip="Close">
							<TextBlock Text="X" Foreground="{DynamicResource TextSecondary}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Button>
					</Grid>

					<StackPanel Margin="0,0,0,16">
						<StackPanel Orientation="Horizontal" Margin="0,0,0,8">
							<TextBlock Text="Name" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="SemiBold"/>
							<TextBlock Text=" *" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold"/>
						</StackPanel>
						<TextBox Style="{StaticResource InputStyle}" Tag="Ex: Diabetes, Allergies..." Text="{Binding NewNom, UpdateSourceTrigger=PropertyChanged}"/>
					</StackPanel>

					<StackPanel Margin="0,0,0,24">
						<TextBlock Text="Description" Foreground="{DynamicResource TextPrimary}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
						<TextBox Style="{StaticResource InputStyle}" Tag="Antecedent description..." Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"/>
					</StackPanel>

					<Button Content="Save" Style="{StaticResource TealBtn}" Height="46" FontSize="15" FontWeight="SemiBold" Command="{Binding SaveCommand}"/>
				</StackPanel>
			</Border>
		</Grid>
	</Grid>
</UserControl>
