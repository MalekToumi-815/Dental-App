<UserControl x:Class="Dental_App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource PrimaryBackground}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        </Style>

        <Style x:Key="SectionBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="Padding" Value="25"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <StackPanel Margin="30">
            <UniformGrid Columns="3" Margin="0,0,0,20" VerticalAlignment="Top">
                <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TotalPatients}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Total Patients" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,10">
                    <StackPanel>
                        <TextBlock Text="{Binding RvToday}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Rendez-Vous aujourd'hui" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="{Binding ConsultsMonth}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Consultations ce mois" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="{Binding ActesTotal}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Actes Médicaux" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="{Binding OrdosActive}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="Ordonnances actives" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding RevenueToday, StringFormat={}{0:N0}}" Foreground="{DynamicResource TextPrimary}" FontSize="28" FontWeight="Bold"/>
                            <TextBlock Text=" DT" Foreground="{DynamicResource TextPrimary}" FontSize="18" VerticalAlignment="Bottom"/>
                        </StackPanel>
                        <TextBlock Text="Recettes du jour" Foreground="{DynamicResource TextSecondary}" Margin="0,5"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Height="400" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource SectionBorderStyle}" Margin="0,0,15,0">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Évolution des consultations" Foreground="{DynamicResource TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>
                        <ItemsControl ItemsSource="{Binding ConsultationChartData}" VerticalAlignment="Bottom">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="12,0" VerticalAlignment="Bottom">
                                        <TextBlock Text="{Binding Value}" Foreground="{DynamicResource AccentColor}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="11" Margin="0,0,0,5"/>
                                        <Border Width="35" Height="{Binding BarHeight}" Background="{DynamicResource AccentColor}" CornerRadius="6,6,2,2" VerticalAlignment="Bottom"/>
                                        <TextBlock Text="{Binding DayName}" Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource SectionBorderStyle}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="Prochain Rendez-Vous Aujourd'hui" Foreground="{DynamicResource TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding UpcomingAppointments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="38" Height="38" Background="{DynamicResource SecondaryBackground}" CornerRadius="19" BorderBrush="{DynamicResource AccentColor}" BorderThickness="2">
                                                <TextBlock Text="{Binding Initials}" Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" Margin="12,0">
                                                <TextBlock Text="{Binding PatientName}" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Time}" Foreground="{DynamicResource TextSecondary}" FontSize="11"/>
                                            </StackPanel>
                                            
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DockPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>