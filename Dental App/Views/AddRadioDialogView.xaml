<UserControl x:Class="Dental_App.Views.AddRadioDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource CardBackground}"
             Width="500">

    <UserControl.Resources>
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Focusable="False" 
                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"
                                          Padding="{TemplateBinding Padding}"
                                          Background="Transparent"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="IsTodayHighlighted" Value="True"/>
            <Setter Property="SelectedDateFormat" Value="Short"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid x:Name="DatePickerRoot">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               Focusable="{TemplateBinding Focusable}"
                                               HorizontalContentAlignment="Stretch"
                                               VerticalContentAlignment="Center"
                                               Background="Transparent"
                                               Foreground="{TemplateBinding Foreground}"
                                               BorderThickness="0"
                                               Padding="12,2"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               CaretBrush="{DynamicResource AccentColor}"/>

                            <Button Grid.Column="1"
                                    x:Name="PART_Button"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="8"
                                    Foreground="{DynamicResource TextPrimary}"
                                    Cursor="Hand"
                                    Focusable="False">
                                <Viewbox Width="20" Height="20">
                                    <Path Data="M 4 2 L 4 4 L 3 4 L 3 21 L 21 21 L 21 4 L 20 4 L 20 2 L 18 2 L 18 4 L 6 4 L 6 2 L 4 2 M 5 8 L 5 20 L 20 20 L 20 8 L 5 8 M 7 10 L 9 10 L 9 12 L 7 12 L 7 10 M 12 10 L 14 10 L 14 12 L 12 12 L 12 10 M 17 10 L 19 10 L 19 12 L 17 12 L 17 10 M 7 14 L 9 14 L 9 16 L 7 16 L 7 14 M 12 14 L 14 14 L 14 16 L 12 16 L 12 14 M 17 14 L 19 14 L 19 16 L 17 16 L 17 14 M 7 18 L 9 18 L 9 20 L 7 20 L 7 18 M 12 18 L 14 18 L 14 20 L 12 20 L 12 18 M 17 18 L 19 18 L 19 20 L 17 20 L 17 18 Z"
                                          Fill="{DynamicResource TextPrimary}"/>
                                </Viewbox>
                            </Button>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   IsOpen="{Binding IsOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource CardBackground}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Margin="0,5,0,0">
                                    <Calendar x:Name="PART_Calendar"
                                              SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}}"
                                              DisplayDate="{Binding DisplayDate, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="{DynamicResource CardBackground}"
                                              Foreground="{DynamicResource TextPrimary}"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource BorderColor}"
            BorderThickness="1"
            CornerRadius="12" Padding="30">

        <StackPanel VerticalAlignment="Top">
            <Grid Margin="0,0,0,20" Height="40">
                <TextBlock Text="Ajouter une Radio"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"
                           VerticalAlignment="Center"/>
                <Button HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="8"
                        Cursor="Hand"
                        Command="{Binding CancelCommand}">
                    <Path Data="M19 6.41L17.59 5L12 10.59L6.41 5L5 6.41L10.59 12L5 17.59L6.41 19L12 13.41L17.59 19L19 17.59L13.41 12L19 6.41Z"
                          Fill="{DynamicResource TextPrimary}" 
                          Width="20" Height="20"/>
                </Button>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="DATE" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text=" *" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold" Margin="2,0,0,0"/>
            </StackPanel>
            <Border Background="{DynamicResource CardBackground}" 
                    BorderBrush="{DynamicResource AccentColor}" 
                    BorderThickness="2" 
                    CornerRadius="6"
                    Margin="0,0,0,12">
                <DatePicker SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                            Style="{StaticResource DialogDatePickerStyle}"/>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="TYPE" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text=" *" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold" Margin="2,0,0,0"/>
            </StackPanel>
            <TextBox Text="{Binding SelectedType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource DialogInputStyle}"
                     Margin="0,0,0,12"
                     Height="45"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="FICHIER" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                <TextBlock Text=" *" Foreground="#EF4444" FontSize="14" FontWeight="SemiBold" Margin="2,0,0,0"/>
            </StackPanel>
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding SelectedFileName, Mode=OneWay}"
                         IsReadOnly="True"
                         Style="{StaticResource DialogInputStyle}"
                         Height="45"/>
                <Button Grid.Column="1"
                        Content="Parcourir"
                        Style="{StaticResource DialogButtonStyle}"
                        Command="{Binding BrowseFileCommand}"
                        Margin="12,0,0,0"
                        Padding="16,12"/>
            </Grid>

            <Rectangle Height="1" Fill="{DynamicResource BorderColor}" Margin="0,0,0,12"/>

            <Button Content="Ajouter"
                    Style="{StaticResource DialogButtonStyle}"
                    Command="{Binding SaveCommand}"
                    Height="50"
                    FontSize="15"
                    IsEnabled="{Binding IsFormValid}"/>
        </StackPanel>
    </Border>
</UserControl>